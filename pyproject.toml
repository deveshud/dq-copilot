[project]
name = "dq-copilot"
version = "0.1.0"
description = "Project scaffold for a multi-agent data quality copilot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pylint>=4.0.5",
]
